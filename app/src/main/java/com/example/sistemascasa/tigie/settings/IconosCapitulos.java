package com.example.sistemascasa.tigie.settings;

import com.example.sistemascasa.tigie.R;

public class IconosCapitulos {

    public IconosCapitulos (){

    }

    public int  IconosCapitulos (String codeChapter) {

        int idCap = R.drawable.cap_01;

        if (codeChapter.equals("01"))
            idCap = R.drawable.cap_01;

        if (codeChapter.equals("02"))
            idCap = R.drawable.cap_02;

        if (codeChapter.equals("03"))
            idCap = R.drawable.cap_03;

        if (codeChapter.equals("04"))
            idCap = R.drawable.cap_04;

        if (codeChapter.equals("05"))
            idCap = R.drawable.cap_05;

        if (codeChapter.equals("06"))
            idCap = R.drawable.cap_06;

        if (codeChapter.equals("07"))
            idCap = R.drawable.cap_07;

        if (codeChapter.equals("08"))
            idCap = R.drawable.cap_08;

        if (codeChapter.equals("09"))
            idCap = R.drawable.cap_09;

        if (codeChapter.equals("10"))
            idCap = R.drawable.cap_10;

        if (codeChapter.equals("11"))
            idCap = R.drawable.cap_11;

        if (codeChapter.equals("12"))
            idCap = R.drawable.cap_12;

        if (codeChapter.equals("13"))
            idCap = R.drawable.cap_13;

        if (codeChapter.equals("14"))
            idCap = R.drawable.cap_14;

        if (codeChapter.equals("15"))
            idCap = R.drawable.cap_15;

        if (codeChapter.equals("16"))
            idCap = R.drawable.cap_16;

        if (codeChapter.equals("17"))
            idCap = R.drawable.cap_17;

        if (codeChapter.equals("18"))
            idCap = R.drawable.cap_18;

        if (codeChapter.equals("19"))
            idCap = R.drawable.cap_19;

        if (codeChapter.equals("20"))
            idCap = R.drawable.cap_20;

        if (codeChapter.equals("21"))
            idCap = R.drawable.cap_21;

        if (codeChapter.equals("22"))
            idCap = R.drawable.cap_22;

        if (codeChapter.equals("23"))
            idCap = R.drawable.cap_23;

        if (codeChapter.equals("24"))
            idCap = R.drawable.cap_24;

        if (codeChapter.equals("25"))
            idCap = R.drawable.cap_25;

        if (codeChapter.equals("26"))
            idCap = R.drawable.cap_26;

        if (codeChapter.equals("27"))
            idCap = R.drawable.cap_27;

        if (codeChapter.equals("28"))
            idCap = R.drawable.cap_28;

        if (codeChapter.equals("29"))
            idCap = R.drawable.cap_29;

        if (codeChapter.equals("30"))
            idCap = R.drawable.cap_30;

        if (codeChapter.equals("31"))
            idCap = R.drawable.cap_31;

        if (codeChapter.equals("32"))
            idCap = R.drawable.cap_32;

        if (codeChapter.equals("33"))
            idCap = R.drawable.cap_33;

        if (codeChapter.equals("34"))
            idCap = R.drawable.cap_34;

        if (codeChapter.equals("35"))
            idCap = R.drawable.cap_35;

        if (codeChapter.equals("36"))
            idCap = R.drawable.cap_36;

        if (codeChapter.equals("37"))
            idCap = R.drawable.cap_37;

        if (codeChapter.equals("38"))
            idCap = R.drawable.cap_38;

        if (codeChapter.equals("39"))
            idCap = R.drawable.cap_39;

        if (codeChapter.equals("40"))
            idCap = R.drawable.cap_40;

        if (codeChapter.equals("41"))
            idCap = R.drawable.cap_41;

        if (codeChapter.equals("42"))
            idCap = R.drawable.cap_42;

        if (codeChapter.equals("43"))
            idCap = R.drawable.cap_43;

        if (codeChapter.equals("44"))
            idCap = R.drawable.cap_44;

        if (codeChapter.equals("45"))
            idCap = R.drawable.cap_45;

        if (codeChapter.equals("46"))
            idCap = R.drawable.cap_46;

        if (codeChapter.equals("47"))
            idCap = R.drawable.cap_47;

        if (codeChapter.equals("48"))
            idCap = R.drawable.cap_48;

        if (codeChapter.equals("49"))
            idCap = R.drawable.cap_49;

        if (codeChapter.equals("50"))
            idCap = R.drawable.cap_50;

        if (codeChapter.equals("51"))
            idCap = R.drawable.cap_51;

        if (codeChapter.equals("52"))
            idCap = R.drawable.cap_52;

        if (codeChapter.equals("53"))
            idCap = R.drawable.cap_53;

        if (codeChapter.equals("54"))
            idCap = R.drawable.cap_54;

        if (codeChapter.equals("55"))
            idCap = R.drawable.cap_55;

        if (codeChapter.equals("56"))
            idCap = R.drawable.cap_56;

        if (codeChapter.equals("57"))
            idCap = R.drawable.cap_57;

        if (codeChapter.equals("58"))
            idCap = R.drawable.cap_58;

        if (codeChapter.equals("59"))
            idCap = R.drawable.cap_59;

        if (codeChapter.equals("60"))
            idCap = R.drawable.cap_60;

        if (codeChapter.equals("61"))
            idCap = R.drawable.cap_61;

        if (codeChapter.equals("62"))
            idCap = R.drawable.cap_62;

        if (codeChapter.equals("63"))
            idCap = R.drawable.cap_63;

        if (codeChapter.equals("64"))
            idCap = R.drawable.cap_64;

        if (codeChapter.equals("65"))
            idCap = R.drawable.cap_65;

        if (codeChapter.equals("66"))
            idCap = R.drawable.cap_66;

        if (codeChapter.equals("67"))
            idCap = R.drawable.cap_67;

        if (codeChapter.equals("68"))
            idCap = R.drawable.cap_68;

        if (codeChapter.equals("69"))
            idCap = R.drawable.cap_69;

        if (codeChapter.equals("70"))
            idCap = R.drawable.cap_70;

        if (codeChapter.equals("71"))
            idCap = R.drawable.cap_71;

        if (codeChapter.equals("72"))
            idCap = R.drawable.cap_72;

        if (codeChapter.equals("73"))
            idCap = R.drawable.cap_73;

        if (codeChapter.equals("74"))
            idCap = R.drawable.cap_74;

        if (codeChapter.equals("75"))
            idCap = R.drawable.cap_75;

        if (codeChapter.equals("76"))
            idCap = R.drawable.cap_76;

        if (codeChapter.equals("78"))
            idCap = R.drawable.cap_78;

        if (codeChapter.equals("79"))
            idCap = R.drawable.cap_79;

        if (codeChapter.equals("80"))
            idCap = R.drawable.cap_80;

        if (codeChapter.equals("81"))
            idCap = R.drawable.cap_81;

        if (codeChapter.equals("82"))
            idCap = R.drawable.cap_83;

        if (codeChapter.equals("83"))
            idCap = R.drawable.cap_83;

        if (codeChapter.equals("84"))
            idCap = R.drawable.cap_84;

        if (codeChapter.equals("85"))
            idCap = R.drawable.cap_85;

        if (codeChapter.equals("86"))
            idCap = R.drawable.cap_86;

        if (codeChapter.equals("87"))
            idCap = R.drawable.cap_87;

        if (codeChapter.equals("88"))
            idCap = R.drawable.cap_88;

        if (codeChapter.equals("89"))
            idCap = R.drawable.cap_89;

        if (codeChapter.equals("90"))
            idCap = R.drawable.cap_90;

        if (codeChapter.equals("91"))
            idCap = R.drawable.cap_91;

        if (codeChapter.equals("92"))
            idCap = R.drawable.cap_92;

        if (codeChapter.equals("93"))
            idCap = R.drawable.cap_93;

        if (codeChapter.equals("94"))
            idCap = R.drawable.cap_94;

        if (codeChapter.equals("95"))
            idCap = R.drawable.cap_95;

        if (codeChapter.equals("96"))
            idCap = R.drawable.cap_96;

        if (codeChapter.equals("97"))
            idCap = R.drawable.cap_97;

        if (codeChapter.equals("98"))
            idCap = R.drawable.cap_98;

        if (codeChapter.equals("99"))
            idCap = R.drawable.cap_99;

        return idCap;
    }

}
